{
  "name": "task-queue-mcp",
  "version": "1.0.0",
  "description": "MCP server for Task Queue — gives an MCP-compatible assistant read/write access to tasks and projects",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run build --prefix ../packages/shared && tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "@task-queue/shared": "file:../packages/shared",
    "firebase-admin": "^13.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.5.0"
  }
}
