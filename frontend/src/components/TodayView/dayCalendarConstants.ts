export const SLOT_HEIGHT = 33;
export const SNAP = 0.25;
export const PX_PER_HOUR = SLOT_HEIGHT * 2;
export const ALL_DAY_ROW_HEIGHT = 24;

/** Start dragging once the pointer moves farther than this (px). */
export const PLACED_TASK_PRE_DRAG_MOVE_PX = 12;
